/**
 * @file
 * JavaScript behaviors for Tailwind Theme.
 */
(function (Drupal) {
  'use strict';

  Drupal.behaviors.tailwindTheme = {
    attach: function (context, settings) {
      // Custom JavaScript behaviors.
    }
  };

})(Drupal);
